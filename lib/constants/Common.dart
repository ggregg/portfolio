import 'package:flutter/material.dart';

const num kInitWidth = 1507;
const num kInitHeight = 864;

const kScreenPadding = EdgeInsets.symmetric(horizontal: 20);
const kColorAssets = [Colors.red, Colors.blue, Colors.green, Colors.yellow];
